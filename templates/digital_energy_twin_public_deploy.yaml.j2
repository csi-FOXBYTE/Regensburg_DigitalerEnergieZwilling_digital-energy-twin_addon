apiVersion: apps/v1
kind: Deployment
metadata:
  name: digital-energy-twin-public
  labels:
    app: digital-energy-twin-public
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digital-energy-twin-public
  template:
    metadata:
      labels:
        app: digital-energy-twin-public
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: public
          image: "{{ digital_energy_twin_public_image }}"
          imagePullPolicy: Always
          securityContext:
            runAsNonRoot: true
            runAsUser: 101
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: {{ digital_energy_twin_public_port | int }}
          volumeMounts:
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
